# Kafka
mp.messaging.outgoing.heartbeat-out.bootstrap.servers=${kafka.bootstrap.servers}
mp.messaging.incoming.heartbeat-in.bootstrap.servers=${kafka.bootstrap.servers}g.heartbeat-out.value.serializer=io.quarkus.kafka.client.serialization.ObjectMapperSerializer